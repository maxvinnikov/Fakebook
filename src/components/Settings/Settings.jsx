import React from 'react';
import s from './Settings.module.css';


const Settings = () => {
    return ( 
        <header className={s.settings}>
          Settings
        </header>
    );
}

export default Settings;